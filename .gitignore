# Secrets — NEVER commit
*.pem
*.key
.env
.env.*
!.env.example
backend/.env*
infra/.env*

# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
build/
*.tgz
*.tar.gz

# Logs
*.log
logs/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local AI / scratch
.claude/
*.tmp
*.swp

# The frontend source/ has its own git repo and is huge (node_modules, dist, ios/build).
# Exclude entirely from this top-level backup. Clone from its remote when needed.
source/

# Base44 export snapshots (already in source)
base44-export/

# Tarballs from deploys
sis*.tar.gz
